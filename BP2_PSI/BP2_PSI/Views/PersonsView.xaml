<Window x:Class="BP2_PSI.Views.PersonsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BP2_PSI.Views"
        mc:Ignorable="d"
        Title="Persons" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button Content="Add" Width="100" Grid.Row="0" HorizontalAlignment="Left" Margin="10" />

        <DataGrid Grid.Row="1" Margin="10" ItemsSource="{Binding Persons}" SelectedItem="{Binding SelectedPerson, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="2" BorderBrush="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Width="*" Header="Id"  Binding="{Binding Id}" />
                <DataGridTextColumn Width="*" Header="First Name"  Binding="{Binding FirstName}" />
                <DataGridTextColumn Width="*" Header="Last Name"  Binding="{Binding LastName}" />
                <DataGridTextColumn Width="*" Header="Birth Date"  Binding="{Binding BirthDate}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>